import 'mocha'
import { expect } from "chai";
import request from 'request';
/*
describe('Pruebas para Añadir funkos', () => {
  it('Test de añadir', () => {
    const url = `http://localhost:3000/funkos?user=test&ID=1&nombre=Ivan Funko&descripcion=Funko Pop de Ivan&tipo=Pop!&genero=Animacion&franquicia=ULL&numeroFranquicia=1&exclusivo=true&caracteristicasEspeciales=Estudiar&valorMercado=28.57`;

    request.post({url: url, json: true, }, (error: Error, response) => {
      expect(response.body).to.be.eql({respuesta: {
          type: 'add',
          success: true,
        }
      });
    });
  })
})

describe('Pruebas para Modificar funkos', () => {
  it('Test de modificar', () => {
    const url = `http://localhost:3000/funkos?user=test&ID=1&nombre=Ivan POP&descripcion=Funko Pop de Ivan&tipo=Pop!&genero=Animacion&franquicia=ULL&numeroFranquicia=1&exclusivo=true&caracteristicasEspeciales=Estudiar&valorMercado=28.57`;

    request.patch({url: url, json: true, }, (error: Error, response) => {
      expect(response.body).to.be.eql({respuesta: {
          type: 'update',
          success: true,
        }
      });
    });
  })
})*/

/*describe('Pruebas para listar funkos', () => {
  it('Test de listar', () => {
    const url = `http://localhost:3000/funkos?user=ivan`;

    request.delete({url: url, json: true, }, (error: Error, response) => {
      console.log(response.body);
      expect(response.body).to.be.eql({respuesta: {
          type: 'list',
          success: true,
          funkoPops: [ [Object], [Object], [Object], [Object], [Object] ]
        }
      });
    });
  })
})*/